# Environment Variable Checklist

Generated by grepping `process.env` across `app/`, `lib/`, `middleware.ts`, and `scripts/`. Vercel Production column reflects what the Vercel CLI reports (see note below); everywhere it says "unknown" means the CLI could not be checked in this pass.

**Vercel CLI status:** not authenticated in this environment. `npx vercel whoami` has no stored credentials (`~/Library/Application Support/com.vercel.cli/config.json` has no token) and hangs waiting on an interactive/browser login rather than failing fast, so `npx vercel env ls` could not be run. No login was attempted (would be a side-effecting auth action outside the scope of a read-only check). Every "Vercel Production" cell below is therefore `unknown` - recommend running `npx vercel login` interactively once, then `npx vercel env ls` to fill this in.

| Variable | Feature it gates | Set in `.env.local`? | Vercel Production | What silently breaks without it |
|---|---|---|---|---|
| `ANTHROPIC_API_KEY` | Claude-generated ad recommendations (`app/api/ads/recommendations/route.ts`), creative strategy generation (`lib/creatives-server.ts`), meeting-prep AI (`lib/meeting-ai.ts`), Upwork proposal AI (`lib/upwork-ai.ts`) | No | unknown | `new Anthropic({ apiKey: undefined })` throws at request time; those routes 500 when called. No graceful "not configured" message. |
| `META_ACCESS_TOKEN` | Meta Marketing API reads (spend/revenue/ROAS sync in `lib/meta.ts`, used by `app/api/cron/meta-sync`) | Yes | unknown | `isMetaConfigured()` returns false; the recommendations flow falls back only if `MOCK_META=1`, otherwise Graph API calls return `{ ok:false, error:'META_ACCESS_TOKEN is not configured' }` and the cron sync silently skips (`{ skipped: true }`). Accounts grid metrics show "Not connected"/"No data yet". |
| `MOCK_META` | Dev fixture for the recommendations flow instead of live Meta calls (`lib/meta.ts`) | No | unknown | Not required; only matters when `META_ACCESS_TOKEN` is dead/being re-authed. Without it, requests just try the real API. |
| `AGENCY_META_AD_ACCOUNT_ID` | House ad account sync into `agency_ads_daily` for the Acquisition dashboard (`app/api/cron/meta-sync/route.ts`) | No | unknown | Agency dashboard metrics stay empty; sync step is skipped (`agency: { synced: false }`), no error surfaced. |
| `GEMINI_API_KEY` | Gemini image generation for creatives (`lib/creatives-images.ts`) | Yes | unknown | `tryGemini()` returns null; falls through to OpenAI if set, otherwise throws `ImageGenError('No image generation key set...')`. |
| `GEMINI_IMAGE_MODEL` | Override for which Gemini image model to try first (`lib/creatives-images.ts`) | No | unknown | Falls back to hardcoded default model list (`gemini-3.1-flash-image`, etc). Not required. |
| `OPENAI_API_KEY` | OpenAI image generation fallback for creatives (`lib/creatives-images.ts`) | No | unknown | `tryOpenAI()` returns null; if `GEMINI_API_KEY` also missing, image generation throws `ImageGenError`. |
| `CRON_SECRET` | Auth for all `/api/cron/*` routes (invoice-reminders, daily-digest, report-approvals, calendar-sync, meta-sync, instantly-sync, metrics-staleness, upwork-search) | No | unknown | See "CRON_SECRET check" section below. If unset, cron routes run with **no auth check at all** (anyone can trigger them). If set but the caller doesn't send the right header, they 401. |
| `AGENT_INBOUND_KEY` | Auth for inbound agent webhooks (`app/api/tasks/inbound`, `app/api/upwork/inbound`, `app/api/questions/inbound`) | No | unknown | If unset, route logs `AGENT_INBOUND_KEY not set` and (per the guard pattern) rejects the request - inbound task/upwork/question creation from external agents breaks entirely. |
| `CAL_WEBHOOK_SECRET` | Cal.com webhook signature verification (`app/api/webhooks/cal/route.ts`) | No | unknown | If unset, `verifySignature()` returns `true` unconditionally - **webhook accepts unsigned requests** (documented in code as "no secret configured - allow all (lock down once live)"). Not a hard break, but a security gap. |
| `STRIPE_SECRET_KEY` | All Stripe API calls (`lib/stripe.ts`) | No | unknown | Key defaults to empty string; any Stripe API call will 401 from Stripe's side. Billing features silently fail. |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook signature verification (`app/api/webhooks/stripe/route.ts`) | No | unknown | If unset, signature check is skipped entirely (`if (secret && ...)`) - webhook accepts unsigned payloads. Security gap, not a hard break. |
| `SIGNWELL_API_KEY` | SignWell e-signature API calls (`lib/signwell.ts`) | No | unknown | Defaults to empty string; contract-sending API calls fail against SignWell (401/invalid auth). |
| `SIGNWELL_TEMPLATE_ID` | Which SignWell template is used for service agreements (`lib/signwell.ts`) | No | unknown | Defaults to empty string; `sendContractFromTemplate` sends a request with an empty `template_id`, which SignWell will reject. |
| `SIGNWELL_TEST_MODE` | Whether SignWell contracts are sent in test mode (`lib/signwell.ts`) | No | unknown | Defaults to `false` (production mode) when unset - contracts sent for real instead of as test drafts. Worth double-checking before go-live. |
| `SIGNWELL_WEBHOOK_ID` | SignWell webhook signature verification (`lib/signwell.ts`) | No | unknown | `verifySignwellEvent()` returns `false` when unset - all incoming SignWell webhook events are rejected as unverified. |
| `RESEND_API_KEY` | Transactional email: meeting invites (`app/api/accounts/[id]/meetings/invite/route.ts`), meeting minutes (`app/api/meetings/[id]/send-minutes/route.ts`), onboarding welcome emails (`lib/onboarding-server.ts`) | No | unknown | Each call site checks for the key and skips sending (logs `RESEND_API_KEY not configured, skipping...` for onboarding); meeting invite/minutes flows report `email_sent: false` instead of erroring. |
| `RESEND_FROM_EMAIL` | From-address for the emails above | No | unknown | Falls back to hardcoded default `August Marketing <team@augustmarketing.co.uk>`. Not required. |
| `SUPABASE_SERVICE_KEY` | Admin (service-role) Supabase client for the main OS project (`lib/supabase-server.ts`, `createSupabaseAdmin()`) - used by nearly every API route including the two new ones (`app/api/callers`, `app/api/accounts`) | Yes | unknown | `createServerClient(url, undefined, ...)` - every admin-privileged query fails; almost the entire app's data layer breaks (leads, calls, accounts, tasks, etc). |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL, used by both the browser client and the server admin/session clients | Yes | unknown | Every Supabase client construction fails immediately (non-null assertion `!` on undefined); app fails to load data anywhere. |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key for session-bound (cookie auth) client (`lib/supabase-server.ts` `createSupabaseServer()`, `middleware.ts`, browser client) | Yes | unknown | Auth/session-aware Supabase calls fail; login/session handling breaks (`middleware.ts` uses this for auth checks). |
| `LEAD_PIPELINE_SUPABASE_URL` | Separate Supabase project for the LinkedIn Ghost lead pipeline (`createLeadPipelineAdmin()` in `lib/supabase-server.ts`) | No | unknown | Throws `Error('Missing LEAD_PIPELINE_SUPABASE_URL / LEAD_PIPELINE_SUPABASE_SERVICE_KEY env vars')` - the LinkedIn dashboard and anything reading `leads`/`lead_activities`/`reply_conversations` breaks. |
| `LEAD_PIPELINE_SUPABASE_SERVICE_KEY` | Same as above, service-role key for that project | No | unknown | Same throw as above. |
| `DATABASE_URL` | Direct Postgres connection string used by the migration runner (`scripts/migrate.js`) | Yes | unknown | Migrations can't run locally/CI; this is a build/deploy-tooling var, not read by the running app itself. |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | Google service-account credentials for both Calendar (`lib/google-calendar.ts`) and Gmail (`lib/google-gmail.ts`) | No | unknown | `isGoogleCalendarConfigured()`/`isGmailConfigured()` return false; calendar sync and Gmail-reading features throw `GoogleCalendarNotConfiguredError`/`GmailNotConfiguredError` when invoked. |
| `GOOGLE_CALENDAR_ID` | Which Google Calendar the app reads/writes (`lib/google-calendar.ts`) | No | unknown | Same as above - calendar features report not-configured; calendar-sync cron has nothing to sync against. |
| `GOOGLE_IMPERSONATE_SUBJECT` | Which mailbox the Gmail service account impersonates (`lib/google-gmail.ts`) | No | unknown | `isGmailConfigured()` false; Gmail-reading features report not-configured. |
| `INSTANTLY_API_KEY` | Instantly.ai cold-email integration (`lib/instantly.ts`), used by `app/api/cron/instantly-sync` | No | unknown | `isInstantlyConfigured()` false; Instantly sync skipped, `Authorization: Bearer undefined` would be sent if called directly (guarded by the configured check upstream). |
| `UPWORK_CLIENT_ID` | Upwork OAuth app identity (`lib/upwork.ts`, `app/api/upwork/oauth/callback/route.ts`) | No | unknown | OAuth callback returns 503 `'UPWORK_CLIENT_ID/SECRET not configured in this environment'`; `isConfigured()` in `lib/upwork.ts` false, so token refresh / job search cron are skipped. |
| `UPWORK_CLIENT_SECRET` | Same OAuth flow, paired with client ID | No | unknown | Same as above. |
| `UPWORK_REFRESH_TOKEN` | Long-lived Upwork OAuth refresh token (`lib/upwork.ts`) | No | unknown | `isConfigured()` false even if client ID/secret are set; Upwork API calls that need a fresh access token fail. |
| `DISCORD_TASKS_WEBHOOK_URL` | Task notifications, and default fallback for pipeline/upwork webhooks if those aren't set (`lib/discord-notify.ts`); also used directly by `app/api/websites/route.ts` and `[id]/route.ts` | No | unknown | Defaults to `''`; fetch to Discord fails silently (fire-and-forget, never awaited in the critical path - comment in code confirms failures can't break the API call itself). No task notifications posted. |
| `DISCORD_ACCOUNTS_WEBHOOK_URL` | Accounts-channel notifications; fallback base for onboarding webhook if unset | No | unknown | Same silent-fail pattern; no accounts notifications posted, and onboarding also loses its fallback target. |
| `DISCORD_PIPELINE_WEBHOOK_URL` | Pipeline-channel notifications (falls back to `DISCORD_TASKS_WEBHOOK_URL` if unset) | No | unknown | Falls back automatically; only matters if you want pipeline notifications in a separate channel from tasks. |
| `DISCORD_ONBOARDING_WEBHOOK_URL` | Onboarding-channel notifications (falls back to `DISCORD_ACCOUNTS_WEBHOOK_URL` if unset) | No | unknown | Falls back automatically; only matters for a dedicated onboarding channel. |
| `DISCORD_UPWORK_WEBHOOK_URL` | Upwork-channel notifications (falls back to `DISCORD_TASKS_WEBHOOK_URL` if unset) | No | unknown | Falls back automatically; only matters for a dedicated Upwork channel. |
| `DISCORD_WEBDEV_WEBHOOK_URL` | Web-dev channel notifications, used directly by `app/api/websites/route.ts` and `[id]/route.ts` | No | unknown | No env-based fallback in `discord-notify.ts` for this one (site code references it directly) - if unset, those specific website notifications are silently skipped/fail. |
| `NEXT_PUBLIC_ONBOARDING_PORTAL_BASE_URL` | Base URL used to build client onboarding portal links (`lib/onboarding-server.ts`) | No | unknown | Falls back to hardcoded `https://augustosv3.vercel.app`. Only matters if the portal is served from a different domain/preview URL. |

## CRON_SECRET check (exact mechanism)

Read directly from `app/api/cron/meta-sync/route.ts` (all other `/api/cron/*` routes use the same pattern):

```ts
const cronSecret = process.env.CRON_SECRET
if (cronSecret) {
  const auth = req.headers.get('authorization')
  if (auth !== `Bearer ${cronSecret}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }
}
```

- It is a **Bearer token in the `Authorization` header**, i.e. `Authorization: Bearer <CRON_SECRET>`.
- The check only runs **if `CRON_SECRET` is set**. If it is not set in the environment, the route accepts any request with no auth at all.
- Vercel's own Cron Jobs feature automatically sends this header using the project's `CRON_SECRET` env var when the cron is configured through `vercel.json` (this repo has a `vercel.json` with cron schedules) - so as long as `CRON_SECRET` is set in Vercel Production, Vercel-triggered runs authenticate automatically. Manually curling a cron endpoint requires setting the same header by hand.

## Research grounding for creative generation (`lib/research-server.ts`)

Concept generation (`app/api/creatives/generate`) is optionally enriched with two external research sources. Both are fully optional and env-gated: if the vars are missing the fetchers return `null` and generation is unchanged (no error). Results are cached in-memory for 24h per client so repeated generations do not re-hit paid APIs.

| Variable | Feature it gates | Set in `.env.local`? | What silently breaks without it |
|---|---|---|---|
| `TRENDTRACK_API_KEY` | TrendTrack winning-ad intelligence for a client's tracked niches/brands (`fetchTrendTrackResearch`). Only fires when the client's `clients.trendtrak_ids` array is non-empty. | Yes (copied from AM Agency workspace `.env`) | `fetchTrendTrackResearch` returns `null`; concepts are grounded on profile data only. No winning-ad hooks/formats injected. |
| `SHOPIFY_ACCESS_TOKEN_<KEY>` | Per-client Shopify Admin token for best-seller lookup (`fetchShopifyResearch`). Read-only (`products.json`). | Partial - `LILLYS` key present but token is a placeholder in the workspace `.env` (`# shpat_... - fill in when available`) | Fetcher returns `null` for that client; real product names/prices are not injected into concept prompts. |
| `SHOPIFY_DOMAIN_<KEY>` | Store domain for the same lookup. Accepts `store`, `store.myshopify.com`, or a full URL. | Yes for `LILLYS` (`lillys-amsterdam.myshopify.com`) | Same as above - no Shopify grounding for that client. |

**Per-client env key convention (`<KEY>`):** derived from the client's name by taking the FIRST word, uppercasing it, and stripping non-alphanumerics (`shopifyEnvKey()` in `lib/research-server.ts`). Both `SHOPIFY_ACCESS_TOKEN_<KEY>` and `SHOPIFY_DOMAIN_<KEY>` must be set for a client's store to be queried. Examples:

- `Lillys Amsterdam` -> `LILLYS` -> `SHOPIFY_ACCESS_TOKEN_LILLYS` / `SHOPIFY_DOMAIN_LILLYS`
- `L'alingi` -> `LALINGI` -> `SHOPIFY_ACCESS_TOKEN_LALINGI` / `SHOPIFY_DOMAIN_LALINGI`

**TrendTrack credit + sort gotchas (mirrored from `execution/scrape_trendtrack.py`):** `/v1/ads` charges 1 credit PER RETURNED ROW, so `fetchTrendTrackResearch` caps total returned rows at ~10 across tracked terms and serves from a 24h cache. `sortBy` must be one of `relevance | newest | longestRunning | reach`; `daysRunning` is INVALID and 400s. We use `longestRunning` to surface proven winners.

## Notes

- Variables not read anywhere in `app/`, `lib/`, `middleware.ts`, or `scripts/` (e.g. any not listed above) were not included even if present in `.env.example` or `.env.local` - this table is grep-derived from actual code usage, not from the example file.
- "Set in `.env.local`?" was checked with `grep -oE "^[A-Z_0-9]+=" .env.local`; a var can be present with an empty value and still count as "Yes" here - worth spot-checking values, not just presence, for anything going live.
